<%= form_for(@current_year) do |f| %>
  <% if @current_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_year.errors.count, "error") %> prohibited this current_year from being saved:</h2>

      <ul>
      <% @current_year.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br />
    <%= f.text_field :year %>
  </div>
  <div class="field">
    <%= f.label :first_week %><br />
    <%= f.text_field :first_week %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
